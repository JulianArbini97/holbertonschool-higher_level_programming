#!/usr/bin/python3
""" To see if is from the class """


def is_same_class(obj, a_class):
    """ True if its from the class, false otherwisw """
    if (type(obj) == a_class):
        return True
    return False
