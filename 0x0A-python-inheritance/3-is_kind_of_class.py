#!/usr/bin/python3
""" the object is an instance of, or an instance of a class that inherited """


def is_kind_of_class(obj, a_class):
    """ funct that determines what is up there """
    return (isinstance(obj, a_class))
